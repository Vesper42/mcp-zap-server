# Test configuration
spring:
  ai:
    mcp:
      server:
        name: mcp-zap-server-test

# Disable security for tests
mcp:
  server:
    apiKey: integration-test-secure-key-12345678
    security:
      enabled: false
    
    # Test configuration
    auth:
      skipValidation: true  # Skip API key validation in tests
      apiKeys:
        - clientId: integration-test-client
          key: integration-test-secure-key-12345678
          description: Integration test client
      
      jwt:
        enabled: true
        secret: test-secret-key-for-jwt-at-least-32-characters-long-required
        issuer: mcp-zap-server-test
        accessTokenExpiry: 3600
        refreshTokenExpiry: 604800

zap:
  server:
    apiKey: test-api-key
  
  scan:
    limits:
      maxActiveScanDurationInMins: 5
      maxSpiderScanDurationInMins: 3
      connectionTimeoutInSecs: 30
    
    url:
      allowLocalhost: true
      allowPrivateNetworks: true
      whitelist: ""
      blacklist: ""

logging:
  level:
    mcp.server.zap: DEBUG
